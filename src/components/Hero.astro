<section class="hero">
  <div class="shell hero-grid">
    <div>
      <div class="eyebrow">Identity-first distributed systems substrate</div>
      <h1>Aster</h1>
      <div class="hero-tagline">RPC after hostnames.</div>
      <p class="hero-copy">Identity-first connectivity, content-addressed contracts, and cross-language services for real distributed systems.</p>
      <p class="hero-subhead">Built for AI, edge, and multi-runtime systems that cannot assume static addresses, central control planes, or bolt-on trust.</p>
      <div class="hero-actions">
        <a class="button primary" href="/architecture">Explore the stack</a>
        <a class="button secondary" href="https://docs.aster.site">Read the docs</a>
      </div>
    </div>

    <aside class="hero-panel">
      <div class="panel-header">
        <span class="meta-label">Protocol-grade overview</span>
        <span class="meta-label">Transport / serialization / contract</span>
      </div>
      <div class="hero-mark-wrap">
        <svg class="octagram" viewBox="0 0 512 512" aria-hidden="true">
          <circle cx="256" cy="256" r="176" stroke="#2A3442" stroke-width="10" opacity="0.42" fill="none"/>
          <polygon points="256.00,106.00 406.00,256.00 256.00,406.00 106.00,256.00" stroke="#78A6FF" stroke-width="20" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
          <polygon points="362.07,149.93 362.07,362.07 149.93,362.07 149.93,149.93" stroke="#78A6FF" stroke-width="20" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
          <polygon points="256.00,140.00 338.02,173.98 372.00,256.00 338.02,338.02 256.00,372.00 173.98,338.02 140.00,256.00 173.98,173.98" stroke="#61D6C2" stroke-width="10" opacity="0.32" stroke-linejoin="round" fill="none"/>
          <polygon points="256.00,194.00 299.84,212.16 318.00,256.00 299.84,299.84 256.00,318.00 212.16,299.84 194.00,256.00 212.16,212.16" fill="#0B0F14" stroke="#61D6C2" stroke-width="12" stroke-linejoin="round"/>
          <circle cx="256" cy="256" r="14" fill="#E6C06B"/>
        </svg>
      </div>
      <div class="panel-body">
        <div class="mini-grid">
          <div class="mini-card">
            <div class="meta-label">Connection model</div>
            <strong>Identity-first transport</strong>
            <p>Dial by who a peer is, not where it happens to live.</p>
          </div>
          <div class="mini-card">
            <div class="meta-label">Contract model</div>
            <strong>Content-addressed truth</strong>
            <p>Portable, deterministic contracts with real identity.</p>
          </div>
          <div class="mini-card">
            <div class="meta-label">Registry model</div>
            <strong>Decentralised publication</strong>
            <p>Discovery without assuming a single control plane.</p>
          </div>
          <div class="mini-card">
            <div class="meta-label">Interaction model</div>
            <strong>Session-scoped services</strong>
            <p>First-class support for stateful, conversational flows.</p>
          </div>
        </div>
      </div>
    </aside>
  </div>
</section>

<style>
  .hero { position: relative; padding: 72px 0 54px; overflow: hidden; }
  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.18fr) minmax(320px, 0.82fr);
    gap: 42px;
    align-items: stretch;
  }
  .eyebrow, .meta-label {
    font-size: 11px; line-height: 1.2; letter-spacing: 0.12em; text-transform: uppercase;
    color: var(--mist-2);
  }
  h1 {
    margin: 0;
    font-size: clamp(56px, 8vw, 92px);
    line-height: 0.94;
    font-weight: 700;
    letter-spacing: -0.065em;
    color: var(--cloud-strong);
  }
  .hero-tagline {
    margin-top: 18px; color: var(--signal); font-weight: 620;
    letter-spacing: -0.03em; font-size: clamp(24px, 2.4vw, 34px); line-height: 1.06;
    width: fit-content; text-wrap: balance;
  }
  .hero-copy {
    margin-top: 22px; max-width: 730px; color: var(--cloud);
    font-size: clamp(20px, 2vw, 24px); line-height: 1.35; letter-spacing: -0.02em;
  }
  .hero-subhead { margin-top: 20px; max-width: 760px; color: var(--mist); font-size: 17px; }
  .hero-actions { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 28px; }

  .hero-panel {
    position: relative; border: 1px solid var(--border-dark); background:
      linear-gradient(180deg, rgba(17, 24, 33, 0.94), rgba(12, 18, 26, 0.9)),
      radial-gradient(circle at 50% 35%, rgba(120, 166, 255, 0.14), transparent 38%);
    box-shadow: var(--shadow-dark); min-height: 560px; display: flex; flex-direction: column;
    border-radius: var(--radius-xl); overflow: hidden;
  }
  .panel-header {
    display: flex; align-items: center; justify-content: space-between; gap: 18px;
    padding: 18px 22px; border-bottom: 1px solid rgba(163, 178, 196, 0.09); background: rgba(255, 255, 255, 0.015);
  }
  .hero-mark-wrap {
    padding: 24px 24px 12px; position: relative; display: grid; place-items: center; min-height: 330px;
  }
  .hero-mark-wrap::before, .hero-mark-wrap::after {
    content: ""; position: absolute; inset: 20px; border-radius: 28px; border: 1px solid rgba(163, 178, 196, 0.06); pointer-events: none;
  }
  .hero-mark-wrap::after { inset: 46px; border-style: dashed; opacity: .5; }
  .octagram { width: min(70%, 340px); aspect-ratio: 1; opacity: 0.98; filter: drop-shadow(0 24px 48px rgba(0, 0, 0, 0.24)); }
  .panel-body { padding: 18px 22px 24px; display: grid; gap: 14px; flex: 1; }
  .mini-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px; }
  .mini-card {
    border-radius: 18px; border: 1px solid rgba(163, 178, 196, 0.09);
    background: rgba(255, 255, 255, 0.025); padding: 14px;
  }
  .mini-card .meta-label { margin-bottom: 8px; }
  .mini-card strong { display: block; font-size: 18px; line-height: 1.15; letter-spacing: -0.02em; color: var(--cloud-strong); }
  .mini-card p { margin: 8px 0 0; font-size: 14px; color: var(--mist); }
  @media (max-width: 1100px) {
    .hero-grid { grid-template-columns: 1fr; }
    .hero-panel { min-height: 0; }
  }
  @media (max-width: 900px) {
    .hero { padding-top: 52px; }
    .hero-tagline { width: auto; max-width: 12ch; }
    .mini-grid { grid-template-columns: 1fr; }
  }
</style>
